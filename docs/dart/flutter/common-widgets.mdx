---
sidebar_label: "Common Widgets"
sidebar_position: 3
---

# Common Widgets

## Layout

### Scaffold
Implements the basic material design visual layout structure.
```dart
Scaffold(
  appBar: AppBar(title: Text('My App')),
  body: Center(child: Text('Hello')),
);
```

### Row
A widget that displays its children in a horizontal array.
```dart
Row(
  children: <Widget>[
    Text('Hello'),
    Text('World'),
  ],
);
```

The `Spacer` widget takes up any available space, pushing adjacent widgets to opposite ends of the main axis.
```dart
Row(
  children: <Widget>[
    Text('Begin'),
    Spacer(), // use Spacer
    Text('Middle'),
    Spacer(), // use Spacer
    Text('End'),
  ],
);
```

### Column
A widget that displays its children in a vertical array.
```dart
Column(
  mainAxisAlignment: MainAxisAlignment.center,
  children: <Widget>[
    Text('Hello'),
    Text('World'),
  ],
);
```

### Stack
A widget that positions its children relative to the edges of its box.
```dart
Stack(
  children: <Widget>[
    Container(color: Colors.blue),
    Center(child: Text('Hello')),
  ],
);
```

### Container
A convenience widget that combines common painting, positioning, and sizing widgets.
```dart
Container(
  width: double.infinity,
  height: double.infinity,
  color: Colors.blue,
  padding: EdgeInsets.all(16.0),
  child: Text('Hello'),
);
```

### SizedBox
A box with a specified size. It can be used to add space between widgets.
```dart
SizedBox(
  width: 200.0,
  height: 300.0,
  child: const Card(child: Text('Hello World')),
)
```

### SingleChildScrollView
A box that can be scrolled. This widget is useful when you have a single widget that is too large to fit on the screen.
```dart
SingleChildScrollView(
  child: Column(
    children: List.generate(
      20,
      (index) => ListTile(
            title: Text('Item $index'),
          ),
        ),
      ),
    )
```

### ListView
A scrollable list of widgets arranged linearly. It is suitable for displaying a long list of items.
```dart
ListView(
  children: <Widget>[
    ListTile(title: Text('Item 1')),
    ListTile(title: Text('Item 2')),
    ListTile(title: Text('Item 3')),
  ],
);
```

**ListView.builder**
A scrollable, linear list of widgets that are created on demand. This is efficient for very long lists as it only builds the widgets that are visible on screen.
```dart
ListView.builder(
  itemCount: 100,
  itemBuilder: (BuildContext context, int index) {
    return ListTile(title: Text('Item $index'));
  },
);
```

### GridView
A scrollable, 2D array of widgets. It displays its children in a grid layout.
```dart
GridView(
  gridDelegate: SliverGridDelegateWithFixedCrossAxisCount(
    crossAxisCount: 2,
    childAspectRatio: 3 / 2,
    crossAxisSpacing: 20,
    mainAxisSpacing: 20,
  ),
  children: [
    Container(color: Colors.red),
    Container(color: Colors.blue),
    Container(color: Colors.green),
    Container(color: Colors.yellow),
  ],
);
```

**GridView.builder**
A scrollable, 2D array of widgets that are created on demand. This is efficient for very large grids as it only builds the widgets that are visible on screen.
```dart
GridView.builder(
  gridDelegate: SliverGridDelegateWithFixedCrossAxisCount(
    crossAxisCount: 2,
    childAspectRatio: 1.0,
    crossAxisSpacing: 10,
    mainAxisSpacing: 10,
  ),
  itemCount: 100,
  itemBuilder: (BuildContext context, int index) {
    return Container(
      decoration: BoxDecoration(
        color: Colors.blue[100 * (index % 9)],
        borderRadius: BorderRadius.circular(8),
      ),
      child: Center(
        child: Text('Item $index'),
      ),
    );
  },
);
```

## Positioning

### Center
A widget that centers its child within itself.
```dart
Center(
  child: Text('Hello'),
);
```

### Padding
A widget that insets its child by a given padding.
```dart
Padding(
  padding: EdgeInsets.all(16.0),
  child: Text('Hello'),
);
```
```dart
Padding(
  padding: EdgeInsets.symmetric(horizontal: 16.0, vertical: 8.0),
  child: Text('Hello'),
);
```

### Expanded
A widget that expands a child of a `Row`, `Column`, or `Flex` so that the child fills the available space.
```dart
Row(
  children: <Widget>[
    Expanded(
      child: Container(
        color: Colors.blue,
        height: 100,
      ),
    ),
    Container(
      color: Colors.red,
      width: 100,
      height: 100,
    ),
  ],
);
```

## Structures

### Button
A material design button. Ex, `TextButton`, `ElevatedButton`, `OutlinedButton`
```dart
ElevatedButton(
  onPressed: () {},
  child: Text('Click Me'),
);
```
<p>Outlined Button</p>
```dart
OutlinedButton(
  onPressed: () {},
  child: Text('Click Me'),
  style: OutlinedButton.styleFrom(
    side: BorderSide(color: Colors.blue),
  ),
);
```
<p>Text Button</p>
```dart
TextButton(
  onPressed: () {},
  child: Text('Click Me'),
  style: TextButton.styleFrom(
    foregroundColor: Colors.red,
  ),
);
```
<p>Elevated Button with custom style</p>
```dart
ElevatedButton(
  onPressed: () {},
  child: Text('Click Me'),
  style: ElevatedButton.styleFrom(
    backgroundColor: Colors.green,
    shape: RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(20),
    ),
  ),
);
```

### DropdownButton
A material design button for selecting from a list of items. The `value` property must be one of the items in the `items` list. The `onChanged` callback is called when the user selects an item.

```dart
List<String> options = ['Option 1', 'Option 2', 'Option 3'];
String? selectedValue = 'Option 1';

DropdownButton<String>(
  value: selectedValue,
  onChanged: (String? newValue) {
    // setState(() {
    //   selectedValue = newValue;
    // });
  },
  items: options.map<DropdownMenuItem<String>>((String value) {
    return DropdownMenuItem<String>(
      value: value,
      child: Text(value),
    );
  }).toList(),
);
```

### Text
A run of text with a single style.
```dart
Text('Hello, world!');
```

### Icon
A graphical icon widget drawn from a font described in an `IconData` class.
```dart
Icon(
  Icons.favorite,
  color: Colors.pink,
  size: 24.0,
);
```

## Styles

### TextStyle
An immutable style describing how to format and paint text.
```dart
Text(
  'Hello',
  style: TextStyle(fontSize: 24.0),
);
```

### Color
An immutable 32-bit color value in ARGB format.
```dart
Container(
  color: Colors.blue,
);
```
